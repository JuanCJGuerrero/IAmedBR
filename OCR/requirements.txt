# OCR Service — Python dependencies
# Python 3.10+

# ── Web framework ──────────────────────────────────────────────────────────
fastapi==0.111.1
uvicorn[standard]==0.30.1
python-multipart==0.0.9        # necessário para UploadFile

# ── Processamento de PDF ───────────────────────────────────────────────────
PyMuPDF==1.24.5                # fitz — converte páginas PDF em imagem

# ── Utilitários ────────────────────────────────────────────────────────────
Pillow==10.4.0

# ── Testes ───────────────────────────────────────────────────────────────
pytest==8.2.2
httpx==0.24.1
