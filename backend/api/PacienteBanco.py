"""
DEPRECATED — substituído por backend/app/routers/pacientes.py.

Este arquivo era uma versão inicial do router de pacientes que importava
módulos `app.database`, `app.models` e `app.schemas` que não existiam.
Agora a estrutura completa do backend vive em `backend/app/`.

Pode ser removido com segurança em um próximo commit.
"""
